requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
openpyxl>=3.1

# Tuy chon: chi can khi bi Cloudflare tra 403 (thuong gap voi IP datacenter).
# Cai them roi chay: python scrape_voz.py --impersonate chrome
# curl_cffi>=0.7
